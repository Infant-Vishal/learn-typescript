let data1: string[] = ["Vishal", "panda", "inderjit"];

let data2: number[] = [1, 2, 3];

let data3: (number | string)[] = ["Vishal", 1, "naruto", 2];

// We can also use the strict value

let seatAllotment: "aisle" | "middle" | "window";

seatAllotment = "aisle";

//seatAllotment = "crew"; // not allowed

export {};
